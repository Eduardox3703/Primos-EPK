---
import { Image } from 'astro:assets';
import logo from '../../images/LOGO-LOS-PRIMOS-BLANCO.png';
import edit from '../../images/losprimosedit.png';
---

<header class="flex justify-center items-center h-20">
  <img
    src={logo.src}
    width="80"
    height="58"
    alt="Logo"
    loading="eager"
  />
</header>

<div class="relative">
  <img
    src={edit.src}
    alt="Header fondo"
    class="img-header"
    loading="eager"
    width="1200"
    height="900"
  />

  <img
    src={logo.src}
    alt="Logo Los Primos Blanco"
    width="600"
    height="195"
    class="absolute bottom-8 left-1/2 transform -translate-x-1/2"
    loading="eager"
  />
</div>

<style>
  .img-header {
    mask-image: radial-gradient(ellipse 100% 70% at center, black 50%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse 100% 70% at center, black 50%, transparent 70%);

    width: 100%;
    height: calc(100vh - 5rem); /* Ajusta según la altura del header */
    object-fit: cover;
    display: block;
  }
</style>
